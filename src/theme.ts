import { ThemeConfig } from "antd";

export const customTheme: ThemeConfig = {
  token: {
    colorPrimary: "#1890ff",  // Couleur principale (bleu Ant Design)
    borderRadius: 4,         // Bords arrondis par défaut
  },
};